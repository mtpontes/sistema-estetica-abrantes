spring.application.name=abrantes

spring.datasource.url=jdbc:mysql://localhost:3306/karol?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

api.security.token.secret=${JWT_SECRET:my-secret-key}


admin.default.username=${ADMIN_USERNAME:root}
admin.default.password=${ADMIN_PASSWORD:root}













#none: Essa � a op��o mais conservadora. O Hibernate n�o faz nenhuma opera��o no esquema do banco de dados. Isso significa que o esquema deve ser gerenciado manualmente e n�o ser� alterado pelo Hibernate.
#validate: O Hibernate valida o esquema do banco de dados em rela��o �s entidades JPA, mas n�o faz altera��es. Ele verifica se as tabelas e colunas necess�rias existem no banco de dados, mas n�o cria ou modifica nada.
#update: Essa op��o permite que o Hibernate atualize automaticamente o esquema do banco de dados conforme necess�rio. Ele cria tabelas para as entidades JPA que ainda n�o existem e faz atualiza��es incrementais nas tabelas existentes para refletir as mudan�as nas entidades.
#create: Com essa op��o, o Hibernate cria o esquema do banco de dados do zero com base nas defini��es das entidades JPA. Ele descarta o esquema existente, se houver, e recria tudo conforme as entidades.
#create-drop: Essa op��o � semelhante a create, mas o Hibernate tamb�m remove o esquema do banco de dados quando a aplica��o � encerrada. � �til para desenvolvimento e testes, mas n�o � adequado para ambientes de produ��o.